export interface PreferenceInterface {
    imageTopics: string[],
    customTopic: string,
}

export interface ThemeInterface {
    mainColor: string,
    backgroundColor: string,
    fontColor: string,
}